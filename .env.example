# 專案根目錄 .env 範例（複製為 .env 後填寫）
# 供 discord-bot 等從根目錄執行的 script 使用

# Discord Application (Bot)
# 從 Discord Developer Portal → Application → Bot / OAuth2 取得
# 並在 Bot → Privileged Gateway Intents 開啟「MESSAGE CONTENT INTENT」
DISCORD_TOKEN=your_bot_token_here
DISCORD_CLIENT_ID=your_application_id_here

# 選填：只註冊到單一伺服器時使用（deploy-commands 會註冊到該 guild）
# 不設則註冊為全球指令（約需 1 小時生效）
# DISCORD_GUILD_ID=your_guild_id_here

# 選填：/prompt 的專案目錄（workspace）。不設 WORKSPACE_DEFAULT 時預設為 bot 啟動時的 cwd
# WORKSPACE_DEFAULT=/path/to/default-project
# WORKSPACE_OTHER_REPO=/path/to/other-repo
# 會出現在 /prompt 的 workspace 選單：default、other-repo
